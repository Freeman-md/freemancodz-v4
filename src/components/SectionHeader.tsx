export function SectionHeader({ eyebrow, title, copy }: { eyebrow?: string; title: string; copy?: string }) { return <div className="mb-10 max-w-3xl">{eyebrow && <p className="mb-3 font-mono text-xs uppercase tracking-[0.2em] text-accent">{eyebrow}</p>}<h2 className="text-3xl font-semibold tracking-[-0.04em] text-black md:text-4xl">{title}</h2>{copy && <p className="mt-4 text-base leading-7 text-muted md:text-lg">{copy}</p>}</div>; }

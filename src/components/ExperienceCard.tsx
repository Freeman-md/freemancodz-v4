import { Chip } from "./Chip";
export function ExperienceCard({ experience }: { experience: any }) { return <article className="rounded border border-line bg-white p-8 transition hover:border-neutral-300 hover:shadow-soft"><div className="mb-6 flex items-center justify-between gap-3"><Chip>{experience.dates}</Chip><span className="font-mono text-xs text-muted">{experience.location}</span></div><h3 className="text-2xl font-semibold tracking-[-0.04em] text-black">{experience.title}</h3><p className="mt-1 text-sm text-muted">{experience.company}</p>{experience.roles && <ul className="mt-4 space-y-1 font-mono text-xs text-ink">{experience.roles.map((r:string)=><li key={r}>{r}</li>)}</ul>}<p className="mt-5 leading-7 text-muted">{experience.focus}</p></article>; }
